# Codex Task: ARCore Adapter

Read `AGENTS.md`, ADR 0003, and capture API docs/code.

Use the `arcore-capture`, `api-verification`, `android-architecture`, and `quality-gate` skills.

Implement the ARCore adapter after verifying official ARCore APIs and dependency versions. Capture timestamps, pose, intrinsics, depth, confidence, tracking state, and runtime support status.

Handle unsupported devices, tracking loss, depth unavailability, and bounded frame queues.

Run targeted tests and document any manual OnePlus 13 validation still required.
