# Codex Task: Create Android Skeleton

Read `AGENTS.md`, `CODEX.md`, ADR 0001, ADR 0002, ADR 0006, and `docs/architecture/module-map.md`.

Use the `android-architecture`, `api-verification`, `compose-ux`, and `quality-gate` skills.

Create the minimal Android/Kotlin/Compose project skeleton only after verifying current official Gradle, Android plugin, Kotlin, and Compose setup details. Add no cloud, analytics, or hidden network dependencies.

Implement a polished first-run shell with scan and recent-project entry points, but no real capture implementation yet.

Run applicable Gradle checks after the skeleton exists.
