# Codex Task: Viewer MVP

Read `AGENTS.md`, ADR 0006, `docs/ux/visual-system.md`, and `docs/ux/product-flow.md`.

Use the `compose-ux`, `filament-viewer`, `api-verification`, and `quality-gate` skills.

Implement a polished touch-first viewer MVP for point cloud or GLB artifacts after verifying Filament and glTF APIs. Include loading, empty, unsupported, and failure states.

Support orbit, pan, zoom, reset, and project metadata display.

Run relevant UI/build checks and inspect the local app when possible.
