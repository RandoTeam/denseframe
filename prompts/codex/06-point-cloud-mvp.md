# Codex Task: Point Cloud MVP

Read `AGENTS.md`, ADR 0005, `docs/reconstruction/pipeline.md`, and `docs/file-format/dfr-v1.md`.

Use the `reconstruction-math`, `raw-project-format`, and `quality-gate` skills.

Implement deterministic depth unprojection and point cloud generation from DFR v1 frames. Document coordinate systems, units, matrix conventions, and serialized reconstruction parameters before code.

Add golden synthetic tests for simple depth planes and known transforms.

Run targeted tests and relevant build checks.
